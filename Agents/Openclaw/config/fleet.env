# Claw-Fleet configuration
# Use this file for setup configuration.
#
# config/fleet.env  — setup configuration
# Values here override config.env/config.local.env defaults. Caller-provided
# environment variables and CLI flags override this file.
#
# Shared infrastructure (model gateway, Opik, package mirrors)
# lives in the repo-root config.env, which the OpenClaw setup, Pinchbench, and
# clawBio runners all read. Set those values there once (BASE_URL, API_KEY,
# MODEL, OPIK_*, mirrors); keep only OpenClaw-specific overrides in this file.

# Fleet size (can also be passed as argument: ./scripts/setup.sh 5)
# COUNT=3

# Directories
# CONFIG_BASE=$HOME/openclaw-instances
# WORKSPACE_BASE=$HOME/openclaw-workspaces
# OPENCLAW_CONFIG_CHMOD=a+rwX
# OPENCLAW_CONFIG_DEFAULT_ACL=true
# OPENCLAW_WORKSPACE_CHMOD=a+rwX
# OPENCLAW_WORKSPACE_DEFAULT_ACL=true

# Resource limits per container
# CPU_LIMIT=2
# MEM_LIMIT=4G

# Service/container naming
# CONTAINER_NAME_PREFIX=openclaw

# Port gap between instances (default: 20, per gateway docs)
# PORT_STEP=20

# Add this offset to the base host gateway port 18789
# DEFAULT_PORTS_OFFSET=0

# Generated OpenClaw config defaults
# SANDBOX_MODE=off
# HEARTBEAT_EVERY=0m
# EXEC_SECURITY=deny
# EXEC_ASK=always

# Generated docker-compose security defaults
# DOCKER_COMPOSE_READ_ONLY=true

# Docker image
# OPENCLAW_IMAGE=openclaw:local

# Timezone
# TZ=Asia/Shanghai

# Opik tracing is controlled by OPIK_URL. The endpoint and credentials
# (OPIK_URL, OPIK_API_KEY, OPIK_WORKSPACE, OPIK_PROJECT_NAME) live in
# config.env; setting OPIK_URL there enables tracing for this fleet.
